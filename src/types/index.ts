// src/types/index.ts
// TypeScript type definitions for the app

export type { Agent, Property, Lead, PropertyType, PropertyCondition, LeadStatus } from '@prisma/client';

// Add custom types here as needed
